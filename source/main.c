#include "../proto/supervisor.h"
// test
int main(int argc, char **argv)
{
    srand(time(NULL));
    int MUCount = 0;
    struct MU *MUs;
    int population = 5;
    MUs = malloc(sizeof(struct MU) * population);

    return 0;
}